/// How often the screen is refreshed (in milliseconds)
pub(crate) const REFRESH_RATE_MILLISECONDS: u64 = 100;

/// Horizontal margin to keep between the terminal edge an the text
pub(crate) const MARGIN_HORIZONTAL: u16 = 1;

/// Vertical margin to keep between the terminal edge an the text
pub(crate) const MARGIN_VERTICAL: u16 = MARGIN_HORIZONTAL;
